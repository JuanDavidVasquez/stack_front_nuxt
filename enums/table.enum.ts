export enum ColumnType {
  TEXT = 'text',
  BUTTON = 'button',
  CHECKBOX = 'checkbox',
  BADGE = 'badge',
  LINK = 'link',
  IMAGE = 'image',
  RATING = 'rating',
  SWITCH = 'switch',
  PROGRESS = 'progress',
  CUSTOM = 'custom',
  DATE = 'date',
  CURRENCY = 'currency',
  ACTIONS = 'actions'
}

export enum Severity {
  SUCCESS = 'success',
  INFO = 'info',
  WARNING = 'warning',
  DANGER = 'danger',
  SECONDARY = 'secondary',
  PRIMARY = 'primary'
}
